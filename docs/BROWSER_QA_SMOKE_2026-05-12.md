# Browser QA Smoke Test ПЕРУКАР

Дата: 2026-05-12

## 1. Як запущено

- `git status --short` перед тестом: clean.
- Локальний сервер: `python -m http.server 8080`.
- URL: `http://localhost:8080/www/index.html`.
- Тестування виконано через браузерний QA в Antigravity.

## 2. Перевірені сценарії

| Сценарій | Статус | Фактична поведінка |
| --- | --- | --- |
| SB-6-83 | PASS | показано warning про Special Blond з бази 6 |
| PREPIG-10-6 | PASS | показано warning/manual decision про значне затемнення зі світлої бази |
| BLACK-EXIT-1 | PASS | показано warning/manual signal про вихід із чорного / темного косметичного пігменту |
| ZONES-ROOT-LENGTH-ENDS | PASS | показано warning про зональне рішення |
| MISSING-CRITICAL-DATA | PASS | показано BLOCKED / недостатньо критичних даних |

## 3. Console errors

- Критичних console errors не виявлено.
- `favicon.ico 404` не вважається критичною помилкою застосунку.

## 4. UX-результат

- Кнопка розрахунку працює.
- Результат рендериться.
- Warning/manual блоки видно в UI.
- При критично неповних даних псевдорецепт із “0” не показується.

## 5. Висновок

Browser QA Smoke Test пройдено.
UI відповідає очікуваній бізнес-логіці guard-сценаріїв.
Файли проєкту під час тестування не змінювались.

## 6. Залишкові обмеження

- Повноцінний `ends_level` ще не реалізований.
- Поточний ZONES guard працює тільки на рівні `root_level` / `length_level`.
- Повна логіка “корінь / довжина / кінці” потребує окремої фази зміни DOM-контракту.
