{
  "_README": "TEMPLATE ONLY - DO NOT INSTALL AS-IS. Remove this _README key before use. VERIFY AGAINST CURRENT CLAUDE CODE DOCS BEFORE INSTALLATION. This is the LOCAL example (.claude/settings.local.json) - per-machine, NOT shared/committed. Local settings override/extend project settings. Use it for machine-specific paths (e.g. absolute pwsh path) and extra personal deny rules. Keep the guards on even locally.",
  "permissions": {
    "deny": [
      "Bash(git push:*)",
      "Bash(git add .:*)",
      "Bash(git add -A:*)",
      "Bash(git add --all:*)"
    ]
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          { "type": "command", "command": "pwsh -NoProfile -File \"$CLAUDE_PROJECT_DIR/.ralfbot/hooks/ralfbot-pretooluse-bash-guard.ps1\"" }
        ]
      },
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          { "type": "command", "command": "pwsh -NoProfile -File \"$CLAUDE_PROJECT_DIR/.ralfbot/hooks/ralfbot-pretooluse-file-guard.ps1\"" }
        ]
      }
    ]
  }
}
